# Animated Mirror

A simple portfolio layout that features a filtered webcam creating a "painted" effect.

## Running

* clone the repo
* `gulp`
* `node index.js`

## Deploying

* clone the repo
* `gulp bundle`
* `node index.js <port>`